import { ICell, IMazeGenerator } from "@/types";

export default class BinaryTreeMaze implements IMazeGenerator {
  generate(grid: ICell[][]): ICell[][] {
    // Implementation for binary tree maze generation
    return grid;
  }
}